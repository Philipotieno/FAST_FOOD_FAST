language: python

python:
  - "3.5"
  - "3.6"
  - "3.7"

install:
  - pip install -r requirements.txt

script: 
  - python -m pytest --cov-report term-missing --cov=app

after_success:
  - coveralls

cache: pip

