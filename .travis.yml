language: python

python:
  - "3.4"
  - "3.5"
  - "3.6"

install:
  - pip install -r requirements.txt

script: 
  - python -m pytest --cov-report term-missing --cov=app
  - coverage run --source=mypkg run.py test

after_success:
  - coveralls

cache: pip

